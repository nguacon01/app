# nginx setup
server {
        listen 8877;
        server_name 192.168.1.16;

        location / {
                include proxy_params;
                proxy_pass http://unix:/home/root/work/web_form_2D/app/app.sock;
        }
}
